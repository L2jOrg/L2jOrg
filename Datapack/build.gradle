plugins {
    id "java"
    id "base"
    id "idea"
    id "eclipse"
}

version '1.0'

dependencies {
    compile project(':Commons')
}

sourceSets {
    main {
        java {
            srcDirs = ['gameserver/data/scripts']
        }
    }
}

dependencies {
    compile project(":Gameserver")
}